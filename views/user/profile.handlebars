</div>
  <div class="col s12 m4">
    <div class="card center-align">
      <div class="card-content">
        <span class="card-title">{{users.firstName}} {{users.lastName}}</span>
        <img src="{{users.image}}" class="circle responsive-img">
      </div>
      <div class="card-action">
        <a href="/stories/user/{{users.id}}">Projects from  {{users.firstName}}</a>
      </div>
       <div class="card-action">
        <h6>About Me: {{users.about}}</h6>
        <a href="/stories//about/{{users.id}}">Edit</a>
      </div>
      <div class="card-action">
        <h6>Contact:  {{users.email}}</h6> 
      </div>
    </div>
  </div>